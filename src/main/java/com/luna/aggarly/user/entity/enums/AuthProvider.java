package com.luna.aggarly.user.entity.enums;

/**
 * Authentication provider indicating how the user signed up (LOCAL, GOOGLE, GITHUB).
 */
public enum AuthProvider {
    LOCAL,
    GOOGLE,
    GITHUB,
    SYSTEM
}
